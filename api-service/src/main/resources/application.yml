server:
  port: 8080

spring:
  application:
    name: api-service
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      properties:
        spring.json.add.type.headers: false

appointments:
  topic-name: appointments
  data-service-base-url: ${DATA_SERVICE_BASE_URL:http://localhost:8081}

---
spring:
  config:
    activate:
      on-profile: docker
  kafka:
    bootstrap-servers: kafka:9092

appointments:
  data-service-base-url: http://data-service:8081

